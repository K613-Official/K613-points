import { describe, it } from 'vitest';

describe('snapshot/manual-bonuses', () => {
  it.todo('parses a valid og-list JSON file');
  it.todo('rejects a file with malformed addresses');
  it.todo('rejects a file with negative amounts');
});
