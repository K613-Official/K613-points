import { describe, it } from 'vitest';

describe('subgraph/queries', () => {
  it.todo('ACTIVE_RESERVES_QUERY returns a non-empty list against a real subgraph');
  it.todo('ATOKEN_HISTORY_QUERY returns preItem when user had a balance before t0');
  it.todo('VTOKEN_HISTORY_QUERY paginates correctly past the 1000-row limit');
});
