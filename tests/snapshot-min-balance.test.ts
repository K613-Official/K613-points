import { describe, it } from 'vitest';

describe('snapshot/min-balance', () => {
  it.todo('returns 0 when preItem is undefined and items is empty');
  it.todo('uses preItem balance as initial when no items in window');
  it.todo('tracks the minimum across multiple intra-window decreases');
  it.todo('is unaffected by deposits made after t0');
});
